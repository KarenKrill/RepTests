# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: DevTesting

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]
  workflow_dispatch: # to run manually

jobs:
  build-and-test:
    name: UbuntuDevTesting
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        dotnetVersion:
          #- 6.0.x
          - 9.0.x
        buildConfig:
          - Debug
          #- Release
    steps:
    - uses: actions/checkout@v6
    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: ${{ matrix.dotnetVersion }}
        cache: true
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build ${{ github.workspace }} --no-restore --configuration ${{ matrix.buildConfig }}
    - name: Test
      run: dotnet test --no-build --configuration ${{ matrix.buildConfig }} --verbosity normal